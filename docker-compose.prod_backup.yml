version: "3.3"

services:
  thalos_web:
    build: .
    command: gunicorn TCS.wsgi:application --bind 0.0.0.0:8005
    volumes:
      - .:/code
    expose:
      - "8005"
    env_file:
      - ./.env.prod
    networks:
      - nginx_network

networks:
  nginx_network:
    external: true